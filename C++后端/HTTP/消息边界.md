# 消息边界

一句话：TCP 是字节流，应用层必须自己判断"一条消息在哪结束"。

## 三种方法
1. **定长**：每条消息固定 N 字节
2. **分隔符**：用特殊序列（如 HTTP 的 `\r\n\r\n`）
3. **消息头带长度**：用 `Content-Length` 指定正文长度

## HTTP 用了两种
分隔符（`\r\n\r\n` 找头部结束）+ 长度（`Content-Length` 定正文）

## 关联
[[粘包与半包]]、[[Buffer]]、[[HTTP报文]]
