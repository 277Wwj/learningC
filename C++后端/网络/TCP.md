# TCP

一句话：TCP 是可靠的、面向连接的**字节流**协议——保证字节顺序和可靠到达，但不保证消息边界。

## 关键特性
- 面向连接：三次握手建连，四次挥手断连
- 可靠：确认、重传、滑动窗口
- 字节流：没有"消息"概念 → 导致 [[粘包与半包]]

## 三次握手 / 四次挥手
- 握手：SYN → SYN-ACK → ACK
- 挥手：FIN → ACK → FIN → ACK
- 短连接慢的根源：每个请求都要握手+挥手（见 [[keep-alive]]）

## 关联
[[粘包与半包]]、[[keep-alive]]、[[阻塞与非阻塞IO]]
